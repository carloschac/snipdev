{
  "name": "snipdev",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run db\" \"npm run api\" \"npm run web\"",
    "api": "cd apps/api && pnpm dev",
    "web": "cd apps/web && pnpm dev",
    "db": "docker compose up -d",
    "db:stop": "docker compose down",
    "build": "cd apps/api && pnpm install && pnpm build",
    "start": "cd apps/api && node dist/server.js"
  }
}
